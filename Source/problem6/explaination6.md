This problem is so easy, I'm not sure about that
Summary:
first: set min , max = first element in array
I iterate array
and check if min > element -> set min again
and if max < element -> set max again

Complex : O(n)
